---
import { Image } from "astro:assets";

interface Props {
	src: string;
	alt: string;
}

const { src, alt } = Astro.props;

// Asegúrate de que `src` sea el camino completo relativo a /src/assets

const images = import.meta.glob<{ default: ImageMetadata }>(
	"/src/assets/**/*.{jpeg,jpg,png,gif,webp}",
);

if (!images[src]) {
	throw new Error(
		`"${src}" does not exist in glob: "src/assets/**/*.{jpeg,jpg,png,gif,webp}"`,
	);
}
---

<div class="relative w-full h-96 overflow-hidden rounded-2xl">
  <div
    class="relative bg-gray-300 w-full h-full cursor-grab active:cursor-grabbing"
    id="image-container"
  >
    <Image
      src={images[src]()}
      alt={alt}
      class="absolute top-0 rounded-2xl left-0 w-full h-auto object-cover"
      id="zoomable-image"
    />
  </div>
</div>
